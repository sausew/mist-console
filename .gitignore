# This is a PRIVATE repo, so personal data (conversation history in data/,
# greeting audio) is committed, not excluded — no privacy carve-outs needed.
# Only transient build/OS junk is ignored (regenerates; not content):
*.log
__pycache__/
.DS_Store
**/.DS_Store
